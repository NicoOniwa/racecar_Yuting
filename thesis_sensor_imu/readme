封装了FDISYSTEMS公司传感器的ROS接口，将传感器数据发布到ROS话题上
只读取了传感器的原始数据，分别为3轴加速度计，3轴陀螺仪和3轴磁力计数据


#下载源码
cd catkin_ws/src
git 
cd ..
catkin_make


#连接Imu后，显示端口号
ls -l /dev/ttyUSB*
#赋予权限
#imu_serial_port*: IMU传感器连接的串口号，每次连接上时需要给该端口赋予权限，运行指令如下：
sudo chmod 777 [imu_serial_port]

#安装依赖项
#ros melodic 版本：
sudo apt-get install ros-melodic-serial
#ros kinetic 版本：
sudo apt-get install ros-kinetic-serial

#启动imu
source catkin_ws/devel/setup.bash
roslaunch FDILink launch_imu.launch
